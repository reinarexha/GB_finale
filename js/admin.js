(function () {
  // Minimal admin script placeholder. Keeps admin footer asset valid.
  var notice = document.querySelector('[data-admin-notice]');
  if (notice) {
    notice.style.display = 'block';
  }
})();
